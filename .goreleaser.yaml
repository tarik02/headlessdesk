version: 2

project_name: headlessdesk

builds:
  - id: headlessdesk-linux
    main: ./cmd/headlessdesk
    binary: headlessdesk
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X headlessdesk/internal/version.Version={{ .Version }}
      - -X headlessdesk/internal/version.Commit={{ .Commit }}
      - -X headlessdesk/internal/version.Date={{ .Date }}
      - -X headlessdesk/internal/version.BuiltBy=goreleaser

  - id: headlessdesk-darwin
    main: ./cmd/headlessdesk
    binary: headlessdesk
    goos:
      - darwin
    goarch:
      - arm64
    env:
      - CGO_ENABLED=1
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X headlessdesk/internal/version.Version={{ .Version }}
      - -X headlessdesk/internal/version.Commit={{ .Commit }}
      - -X headlessdesk/internal/version.Date={{ .Date }}
      - -X headlessdesk/internal/version.BuiltBy=goreleaser

  - id: headlessdesk-windows-mingw
    main: ./cmd/headlessdesk
    binary: headlessdesk
    goos:
      - windows
    goarch:
      - amd64
    env:
      - CGO_ENABLED=1
      - CC=x86_64-w64-mingw32-gcc-posix
      - CXX=x86_64-w64-mingw32-g++-posix
      - PKG_CONFIG_ALLOW_CROSS=1
      - CGO_CPPFLAGS=-D__STDC_NO_THREADS__ -D__STDC_NO_ATOMICS__
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X headlessdesk/internal/version.Version={{ .Version }}
      - -X headlessdesk/internal/version.Commit={{ .Commit }}
      - -X headlessdesk/internal/version.Date={{ .Date }}
      - -X headlessdesk/internal/version.BuiltBy=goreleaser

archives:
  - ids:
      - headlessdesk-linux
      - headlessdesk-darwin
      - headlessdesk-windows-mingw
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt
